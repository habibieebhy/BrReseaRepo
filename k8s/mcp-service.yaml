apiVersion: v1
kind: Service
metadata:
  name: brixta-mcp-service
  namespace: default
  labels:
    app: brixta-mcp
spec:
  type: ClusterIP
  selector:
    app: brixta-mcp
  ports:
    - name: mcp-http
      port: 80
      targetPort: 8001
